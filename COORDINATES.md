# 좌표로 크기·방향 정하기

## 새 램프 생성

세 좌표를 사용합니다. 첫 두 점은 힌지 쪽 가장자리의 양끝 블록, 세 번째 점은 앞쪽 끝 블록입니다. 세 점의 Y는 같은 데크 높이로 지정합니다. 아래 한 칸에도 보강재가 생성됩니다.

```mcfunction
/ramp demo cargo 100 70 100 115 70 100 100 70 121
```

이 예시는 **폭 16 × 길이 22**, **+Z 방향**의 436블록 램프를 만듭니다. 양끝을 포함하므로 `115 - 100 + 1 = 16`입니다. 힌지는 데크 윗면 `(108, 71, 100)`에 자동으로 놓입니다.

X 방향으로 뻗게 하려면 힌지 두 점을 Z축에 나란히 지정합니다. 다음은 **폭 8 × 길이 12**, **-X 방향**입니다.

```mcfunction
/ramp demo cargo_west 100 70 100 100 70 107 89 70 100
```

현재 위치 기준 상대좌표도 됩니다.

```mcfunction
/ramp demo cargo ~ ~ ~ ~15 ~ ~ ~ ~ ~21
```

- 힌지 양끝은 X 또는 Z 중 한 축으로만 달라야 합니다.
- 앞쪽 끝은 힌지의 폭 안에서, 힌지와 다른 수평축 방향으로 떨어져 있어야 합니다.
- 폭·길이는 각각 2~256이지만, 보강재를 포함한 실제 블록 수는 `max_blocks_per_ramp`(기본 512) 이하이어야 합니다. 크게 만들려면 config 상한도 올려야 합니다.
- 생성할 위치는 모두 비어 있고 청크가 로드되어 있어야 합니다. 좌표는 닫힌 수평 데크의 크기이며 두께는 기존 디자인대로 최대 2블록입니다.

## 기존 구조물 선택

기존 블록을 선택하는 경우 두 모서리로 캡처합니다. 좌표 순서는 상관없으며 AIR는 제외합니다. 이 명령은 실제 블록을 확대·축소하지 않습니다.

```mcfunction
/ramp create custom
/ramp capture custom 100 69 100 115 70 121
/ramp hinge custom 108 71 100
```

기존 `pos1` / `pos2` / `capture` 방식도 계속 사용할 수 있습니다. 직접 좌표를 주는 캡처는 콘솔에서도 동작합니다.

## 방향과 각도

힌지에서 바라볼 목표 좌표를 입력합니다.

```mcfunction
/ramp direction custom 108 61 116
```

힌지 `(108,71,100)`에서 **+Z 16, 아래 10**인 점이므로 +Z 방향, 아래쪽, 약 32도로 설정됩니다. 자동 축 판정은 해제됩니다. 이후:

```mcfunction
/ramp open custom
/ramp close custom
```

목표 좌표는 **방향 기준점**입니다. 램프를 늘리거나 끝을 그 점에 정확히 맞추는 기능은 아닙니다. 크기는 생성/캡처 좌표로 정합니다.

- 목표 Y가 힌지 Y와 같으면 현재 각도와 위/아래 설정을 유지하며 수평 방향만 바꿉니다.
- Y가 다르면 `atan2(높이 차이, 수평 거리)`로 각도를 계산합니다. 허용 각도는 10~90도입니다.
- 기존 엔진처럼 X/Z축 정렬을 사용합니다. X/Z가 함께 바뀌면 차이가 더 큰 축을 선택합니다. 정확한 대각선이나 수직으로만 떨어진 점은 거부합니다.
- 좌표 캡처와 방향 변경은 **닫힌 상태**, **램프와 같은 차원**에서 실행합니다.
- `~`는 힌지가 아니라 명령 실행자 기준 상대좌표입니다. 콘솔/커맨드 블록은 `/execute in ... positioned ... run ramp ...`도 사용할 수 있습니다.
- 기존 `/ramp direction custom down`, `/ramp axis`, `/ramp extend`, `/ramp angle`도 유지됩니다.

## 여러 개 설치

`Ramp already exists`는 같은 이름을 다시 쓴 경우의 오류였습니다. 0.2.1부터는 `create`와 `demo`가 자동으로 비어 있는 이름을 선택합니다.

```mcfunction
/ramp create cargo
/ramp create cargo
/ramp create cargo
```

위 명령은 비어 있는 이름부터 `cargo`, `cargo_2`, `cargo_3`을 만듭니다. **메시지에 표시된 실제 ID**로 캡처·열기·연결합니다. 기존 램프는 덮어쓰지 않습니다.

`/ramp demo cargo ...`도 같은 규칙이며, `/ramp demo`는 `ramp`, `ramp_2`, ...를 자동 생성합니다. 새 램프는 다른 빈 위치에 설치해야 합니다.

```mcfunction
/ramp list
/ramp open cargo
/ramp open cargo_2
/ramp bind cargo_2
```

각 램프에는 별도의 컨트롤러를 연결할 수 있습니다. 설치 개수 1개 제한은 없으며, 기본 **동시 애니메이션 4개** 제한은 유지합니다. 다른 램프의 블록이나 통행면과 실제 이동 공간이 겹치면 개폐를 거부합니다.

## 업데이트

Minecraft를 종료한 뒤 `mods`의 기존 RampDoor JAR를 `build/libs/rampdoor-0.2.1.jar`로 교체합니다. 버전이 다른 RampDoor JAR 둘을 동시에 넣지 않습니다. v0.2 저장 형식은 유지됩니다.
